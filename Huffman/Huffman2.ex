defmodule Huffman do
	def freq(sample) do
		freq=[]
		freq(sample, ][)
	end

	def freq(sample,freq) do
		
	end

	def freq([], freq) do
		
	end
	def freq([char | rest], freq) do

	freq(rest, ...)
	end	

end